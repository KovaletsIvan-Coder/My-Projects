import { createContext } from "react";
export const amountItemInBasketContext = createContext(null);
export const ulBasketContext = createContext(null); //контекст для передавання елементів в кошик
export const visibleBasketIconContext = createContext(true);
